{
  "name": "ResidenceApp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-backend": "cd server && npm install",
    "install-frontend": "cd client && npm install",
    "start:backend": "cd server && npm start",
    "start:frontend": "cd client && npm start",
    "setup:install": "concurrently \"npm run install-backend\" \"npm run install-frontend\"",
    "setup:start": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
